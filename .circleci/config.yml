version: 2
jobs:
  one:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Android build
          command: |
            apt install python -y
            source j2v8-cli.sh
            nodejs git clone
            nodejs diff apply
            python build.py --target android --docker
  two:
    docker:
      - image: docker:17.05.0-ce-git
    steps:
      - checkout
      - run: echo "hi"
workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two
